kind: AdaptiveDialog
modelDescription: Use when the user asks for any US $ figure — net sales, cost, gross margin, OPEX, GTN,operating income, revenue, or growth/YoY in dollars.
beginDialog:
  kind: OnRecognizedIntent
  id: main
  intent: {}
  actions:
    - kind: SetVariable
      id: setVariable_srcdF2
      displayName: Set Originalq
      variable: Topic.FinanceOriginalQ
      value: =System.Activity.Text
    - kind: ConditionGroup
      id: conditionGroup_Hn6pYB
      conditions:
        - id: conditionItem_DwFFHz
          condition: |-
            =IsMatch(
                Lower(Topic.FinanceOriginalQ),
                "q1|q2|q3|q4|quarter|ytd|year to date|qtd|mtd|month|monthly|full year|outlook|half|h1|h2|january|february|march|april|june|july|august|september|october|november|december|cpc",
                MatchOptions.Contains
            )
          actions:
            - kind: SetVariable
              id: setVariable_BoD0Xn
              variable: Topic.Query
              value: =Topic.FinanceOriginalQ
            - kind: SetVariable
              id: setVariable_askedScope
              variable: Topic.AskedScope
              value: |-
                =If(
                    IsMatch(Lower(Topic.FinanceOriginalQ), "full year|fy|outlook|annual|latest outlook|mar lo", MatchOptions.Contains), "full_year",
                    IsMatch(Lower(Topic.FinanceOriginalQ), "ytd|year to date|qtd|q1|q2|q3|q4|quarter|half|h1|h2|cpc", MatchOptions.Contains), "ytd",
                    IsMatch(Lower(Topic.FinanceOriginalQ), "month|monthly|mtd|january|february|march|april|june|july|august|september|october|november|december", MatchOptions.Contains), "month",
                    "ytd"
                )
            - kind: SearchAndSummarizeContent
              id: 6W022d
              userInput: =Topic.Query
              additionalInstructions: |-
                You answer US financial questions ONLY from the retrieved Financial Results content (US monthly Financial Close decks) - the source of truth for $ figures: Net Sales, Cost, Gross Margin, OPEX, Operating Income \*\*across monthly (e.g. 2026 03), year to date / quarter (e.g. Q1 2026 March YTD), and full year outlook (FY Latest Outlook / Mar LO) periods.\*\* GROUNDING: Quote numbers VERBATIM (USD). Never fabricate, compute, or estimate. If the retrieved content does not contain the figure, say "I do not have that in the indexed financial results" and stop. \*\*FULL YEAR / OUTLOOK: For a full year question (FY, full year, outlook, Latest Outlook, Mar LO), the FY value from the "FY … Latest Outlook / Mar LO by Brand" table (`measure_basis = outlook_lo`, `period_scope = full_year`) IS the correct answer — report it and label it clearly as "FY outlook (Latest Outlook / Mar LO)". This is the ONE case where an outlook value is the answer; do NOT say "not found" when an FY outlook figure exists in the sources.\*\* RANKING ("which brands / top / highest / growth"): output a markdown table with EXACTLY these three columns: Brand | Net Sales ($m) | vs PY (%). The THIRD column is ALWAYS the printed vs-prior-year PERCENTAGE (a % value such as +58%) - NEVER an absolute $ growth number, and NEVER a column titled "Growth (m)".EveryrowMUSTshowBOTHtheNetSales_m_)"._EveryrowMUSTshowBOTHtheNetSales_ value AND the vs PY %. Do NOT compute or display absolute growth.NEVERuseabar−chart"Datapoints"/"growth="value(e.g.45,12)−thosearechart−labelARTIFACTSandareWRONG;readONLYthemarkdownTABLE′sNetSalesandvsPY_growth_._NEVERuseabar_−_chart_"_Datapoints_"/"_growth_="_value_(_e_._g_.45,12)−_thosearechart_−_labelARTIFACTSandareWRONG_;_readONLYthemarkdownTABLE_′_sNetSalesandvsPY_ growth figure is not reliably printed. Rank by vs PY % when the user asks for highest/fastest growth; otherwise rank by Net Sales magnitude. NET SALES = the currency row (m/USDmillions)attheaskedscope+actual∗∗(or‘outlooklo‘forafull yearoutlookquestion)∗∗,fortheEXACTbrandasked−NOTaUS−total/aggregate,NOTaTarget,NOTavs−TGT/vs−PYvarianceline.Leadwiththe_m_/_USDmillions_)_attheaskedscope_+_actual_∗∗(_or_‘_outlooklo_‘_forafull_ _yearoutlookquestion_)∗∗,_fortheEXACTbrandasked_−_NOTaUS_−_total_/_aggregate_,_NOTaTarget_,_NOTavs_−_TGT_/_vs_−_PYvarianceline_._Leadwiththe_ headline, then add vs PY% if asked. GLOSSARY: GTN=Gross-to-Net; PVM=Price Volume Mix; PY=Prior Year; TGT=Target; LO=Latest Outlook (a forecast - not a monthly _actual_, **but it IS the correct figure when the user asks for the full year / FY outlook**); CPC=Core Profit Contribution (a P&L profit line, NOT the PVM contribution buckets). Match the exact metric and unit; never give a % when asked for a (ora(_ora_ when asked for a %). Assume US geography and USD; state that assumption.
                PERIOD DEFAULT (never ask first): If the user does NOT name a period (e.g. "current", "latest", or no period word at all), answer with the QUARTER figure (March YTD, period_scope ytd) and label it "Q1 2026 (March YTD)". Answer a single month ONLY when the user explicitly says month / MTD / a month name; answer full-year ONLY when they say FY / full year / outlook / Latest Outlook / Mar LO.
                Do NOT add any follow-up offer about other periods; a separate Question node handles that. End your answer after the figure(s).
                COMPLETENESS: when the answer is a ranking or list, include EVERY brand row present in the retrieved table, top to bottom, in ranked order - do NOT truncate to a "top 5", collapse, or drop rows to be concise unless the user explicitly asked for "top N".
                Answer the user's question:{Topic.Query}
              fileSearchDataSource:
                searchFilesMode:
                  kind: DoNotSearchFiles
              knowledgeSources:
                kind: SearchSpecificKnowledgeSources
                knowledgeSources:
                  - cr1df_FinSightUSV2.topic.FinSightUSV2AzureAISearch_H2igSam_KOdvKH_SmGW4f
              applyModelKnowledgeSetting: false
      elseActions:
        - kind: SetVariable
          id: setVariable_kaiBVS
          variable: Topic.Query1
          value: =Topic.FinanceOriginalQ & " year to date (YTD) Q1 period_scope ytd"
        - kind: SetVariable
          id: setVariable_askedScopeElse
          variable: Topic.AskedScope
          value: ="ytd"
        - kind: SearchAndSummarizeContent
          id: 43VLVH
          variable: Topic.FinanaceAnswer2
          userInput: =Topic.Query1
          additionalInstructions: |-
            You answer US financial questions ONLY from the retrieved Financial Results content (US monthly Financial Close decks) - the source of truth for $ figures: Net Sales, Cost, Gross Margin, OPEX, Operating Income \*\*across monthly (e.g. 2026 03), year to date / quarter (e.g. Q1 2026 March YTD), and full year outlook (FY Latest Outlook / Mar LO) periods.\*\* GROUNDING: Quote numbers VERBATIM (USD). Never fabricate, compute, or estimate. If the retrieved content does not contain the figure, say "I do not have that in the indexed financial results" and stop. \*\*FULL YEAR / OUTLOOK: For a full year question (FY, full year, outlook, Latest Outlook, Mar LO), the FY value from the "FY … Latest Outlook / Mar LO by Brand" table (`measure_basis = outlook_lo`, `period_scope = full_year`) IS the correct answer — report it and label it clearly as "FY outlook (Latest Outlook / Mar LO)". This is the ONE case where an outlook value is the answer; do NOT say "not found" when an FY outlook figure exists in the sources.\*\* RANKING ("which brands / top / highest / growth"): output a markdown table with EXACTLY these three columns: Brand | Net Sales ($m) | vs PY (%). The THIRD column is ALWAYS the printed vs-prior-year PERCENTAGE (a % value such as +58%) - NEVER an absolute $ growth number, and NEVER a column titled "Growth (m)".EveryrowMUSTshowBOTHtheNetSales_m_)"._EveryrowMUSTshowBOTHtheNetSales_ value AND the vs PY %. Do NOT compute or display absolute growth.NEVERuseabar−chart"Datapoints"/"growth="value(e.g.45,12)−thosearechart−labelARTIFACTSandareWRONG;readONLYthemarkdownTABLE′sNetSalesandvsPY_growth_._NEVERuseabar_−_chart_"_Datapoints_"/"_growth_="_value_(_e_._g_.45,12)−_thosearechart_−_labelARTIFACTSandareWRONG_;_readONLYthemarkdownTABLE_′_sNetSalesandvsPY_ growth figure is not reliably printed. Rank by vs PY % when the user asks for highest/fastest growth; otherwise rank by Net Sales magnitude. NET SALES = the currency row (m/USDmillions)attheaskedscope+actual∗∗(or‘outlooklo‘forafull yearoutlookquestion)∗∗,fortheEXACTbrandasked−NOTaUS−total/aggregate,NOTaTarget,NOTavs−TGT/vs−PYvarianceline.Leadwiththe_m_/_USDmillions_)_attheaskedscope_+_actual_∗∗(_or_‘_outlooklo_‘_forafull_ _yearoutlookquestion_)∗∗,_fortheEXACTbrandasked_−_NOTaUS_−_total_/_aggregate_,_NOTaTarget_,_NOTavs_−_TGT_/_vs_−_PYvarianceline_._Leadwiththe_ headline, then add vs PY% if asked. GLOSSARY: GTN=Gross-to-Net; PVM=Price Volume Mix; PY=Prior Year; TGT=Target; LO=Latest Outlook (a forecast - not a monthly _actual_, **but it IS the correct figure when the user asks for the full year / FY outlook**); CPC=Core Profit Contribution (a P&L profit line, NOT the PVM contribution buckets). Match the exact metric and unit; never give a % when asked for a (ora(_ora_ when asked for a %). Assume US geography and USD; state that assumption.
            PERIOD DEFAULT (never ask first): If the user does NOT name a period (e.g. "current", "latest", or no period word at all), answer with the QUARTER figure (March YTD, period_scope ytd) and label it "Q1 2026 (March YTD)". Answer a single month ONLY when the user explicitly says month / MTD / a month name; answer full-year ONLY when they say FY / full year / outlook / Latest Outlook / Mar LO.
            Do NOT add any follow-up offer about other periods; a separate Question node handles that. End your answer after the figure(s).
            COMPLETENESS: when the answer is a ranking or list, include EVERY brand row present in the retrieved table, top to bottom, in ranked order - do NOT truncate to a "top 5", collapse, or drop rows to be concise unless the user explicitly asked for "top N".
            Answer the user's question:{Topic.Query1}
          fileSearchDataSource:
            searchFilesMode:
              kind: DoNotSearchFiles
          knowledgeSources:
            kind: SearchSpecificKnowledgeSources
            knowledgeSources:
              - cr1df_FinSightUSV2.topic.FinSightUSV2AzureAISearch_H2igSam_KOdvKH_SmGW4f
          applyModelKnowledgeSetting: false
          responseCaptureType: FullResponse
    - kind: ConditionGroup
      id: conditionGroup_offer
      conditions:
        - id: cond_askedMonth
          condition: |-
            =Topic.AskedScope = "month"
          actions:
            - kind: Question
              id: question_offerQuarter
              interruptionPolicy:
                allowInterruption: true
              alwaysPrompt: true
              variable: Topic.FollowupReply
              prompt: |-
                Would you like the quarter (Q1 / March YTD) figure for the same brands too? Reply Yes or No - or just ask another question.
              entity: StringPrebuiltEntity
            - kind: ConditionGroup
              id: conditionGroup_followupQuarter
              conditions:
                - id: cond_noQuarter
                  condition: |-
                    =Lower(Trim(Topic.FollowupReply)) in ["no","nope","nah","no thanks","not now","that's all","thats all","nothing","done","stop","i'm good","im good"]
                  actions:
                    - kind: SendActivity
                      id: sendActivity_noQuarter
                      activity:
                        text:
                          - Okay - let me know if you'd like another brand or period.
                - id: cond_yesQuarter
                  condition: |-
                    =Lower(Trim(Topic.FollowupReply)) in ["yes","y","yeah","yep","yup","sure","ok","okay","please","yes please","go ahead","do it","absolutely","correct","quarter","ytd"]
                  actions:
                    - kind: SetVariable
                      id: setVariable_query2Quarter
                      variable: Topic.Query2
                      value: =Topic.FinanceOriginalQ & " - now give the same metric and the SAME subject (same brand or brands) as above, at the same level of detail, but for the quarter (Q1 / March YTD, year to date) period instead. Do not broaden to all brands and do not add commentary."
                    - kind: SearchAndSummarizeContent
                      id: search_oppQuarter
                      userInput: =Topic.Query2
                      additionalInstructions: |-
                        You answer US financial questions ONLY from the retrieved Financial Results content (US monthly Financial Close decks) - the source of truth for $ figures (Net Sales, Cost, Gross Margin, OPEX, Operating Income). GROUNDING: Quote numbers VERBATIM (USD). Never fabricate, compute, or estimate. If the retrieved content does not contain the figure, say "I do not have that in the indexed financial results" and stop. MIRROR THE PREVIOUS ANSWER'S SHAPE EXACTLY - same subject (same brand or brands), same metric, same level of detail. If the previous answer was a SINGLE figure for ONE brand, return ONLY that one brand's figure for the new period; do NOT expand to a brand-wide list and do NOT add a 'comments by brand' or commentary narrative unless the user explicitly asked for it. Keep the SAME format as the previous answer - if it was a ranking table (Brand | Net Sales ($m) | vs PY (%)) keep that table with every row showing BOTH values; if it was one value, return just that one value. COMPLETENESS: if the previous answer was a ranking/list, include EVERY brand that appeared in it, top to bottom - do NOT drop or truncate rows to be concise. Show each brand's figure for the new period; if a brand from the previous answer has no figure in the retrieved data for the new period, keep its row and mark it "not in the indexed data" rather than silently omitting it. Assume US geography and USD. Do NOT add any follow-up offer about other periods. Answer the user's question:{Topic.Query2}
                      fileSearchDataSource:
                        searchFilesMode:
                          kind: DoNotSearchFiles
                      knowledgeSources:
                        kind: SearchSpecificKnowledgeSources
                        knowledgeSources:
                          - cr1df_FinSightUSV2.topic.FinSightUSV2AzureAISearch_H2igSam_KOdvKH_SmGW4f
                      applyModelKnowledgeSetting: false
              elseActions:
                - kind: SetVariable
                  id: setVariable_newQQuarter
                  variable: Topic.Query3
                  value: =Topic.FollowupReply & " (same brands and context as the previous answer)"
                - kind: SearchAndSummarizeContent
                  id: search_newQQuarter
                  userInput: =Topic.Query3
                  additionalInstructions: |-
                    You answer US financial questions ONLY from the retrieved Financial Results content (US monthly Financial Close decks) - the source of truth for $ figures (Net Sales, Cost, Gross Margin, OPEX, Operating Income). GROUNDING: Quote numbers VERBATIM (USD). Never fabricate, compute, or estimate. If the retrieved content does not contain the figure, say "I do not have that in the indexed financial results" and stop. Keep the SAME format as the previous answer (rankings: Brand | Net Sales ($m) | vs PY (%), every row showing BOTH the Net Sales and the vs PY %). Assume US geography and USD. Do NOT add any follow-up offer about other periods. Answer the user's question:{Topic.Query3}
                  fileSearchDataSource:
                    searchFilesMode:
                      kind: DoNotSearchFiles
                  knowledgeSources:
                    kind: SearchSpecificKnowledgeSources
                    knowledgeSources:
                      - cr1df_FinSightUSV2.topic.FinSightUSV2AzureAISearch_H2igSam_KOdvKH_SmGW4f
                  applyModelKnowledgeSetting: false
        - id: cond_askedYtd
          condition: |-
            =Topic.AskedScope = "ytd"
          actions:
            - kind: Question
              id: question_offerMonth
              interruptionPolicy:
                allowInterruption: true
              alwaysPrompt: true
              variable: Topic.FollowupReply
              prompt: |-
                Would you like the single-month (MTD) figure for the same brands too? Reply Yes or No - or just ask another question.
              entity: StringPrebuiltEntity
            - kind: ConditionGroup
              id: conditionGroup_followupMonth
              conditions:
                - id: cond_noMonth
                  condition: |-
                    =Lower(Trim(Topic.FollowupReply)) in ["no","nope","nah","no thanks","not now","that's all","thats all","nothing","done","stop","i'm good","im good"]
                  actions:
                    - kind: SendActivity
                      id: sendActivity_noMonth
                      activity:
                        text:
                          - Okay - let me know if you'd like another brand or period.
                - id: cond_yesMonth
                  condition: |-
                    =Lower(Trim(Topic.FollowupReply)) in ["yes","y","yeah","yep","yup","sure","ok","okay","please","yes please","go ahead","do it","absolutely","correct","month","monthly","mtd"]
                  actions:
                    - kind: SetVariable
                      id: setVariable_query2Month
                      variable: Topic.Query2
                      value: =Topic.FinanceOriginalQ & " - now give the same metric and the SAME subject (same brand or brands) as above, at the same level of detail, but for the single month (MTD, March) period instead. Do not broaden to all brands and do not add commentary."
                    - kind: SearchAndSummarizeContent
                      id: search_oppMonth
                      userInput: =Topic.Query2
                      additionalInstructions: |-
                        You answer US financial questions ONLY from the retrieved Financial Results content (US monthly Financial Close decks) - the source of truth for $ figures (Net Sales, Cost, Gross Margin, OPEX, Operating Income). GROUNDING: Quote numbers VERBATIM (USD). Never fabricate, compute, or estimate. If the retrieved content does not contain the figure, say "I do not have that in the indexed financial results" and stop. MIRROR THE PREVIOUS ANSWER'S SHAPE EXACTLY - same subject (same brand or brands), same metric, same level of detail. If the previous answer was a SINGLE figure for ONE brand, return ONLY that one brand's figure for the new period; do NOT expand to a brand-wide list and do NOT add a 'comments by brand' or commentary narrative unless the user explicitly asked for it. Keep the SAME format as the previous answer - if it was a ranking table (Brand | Net Sales ($m) | vs PY (%)) keep that table with every row showing BOTH values; if it was one value, return just that one value. COMPLETENESS: if the previous answer was a ranking/list, include EVERY brand that appeared in it, top to bottom - do NOT drop or truncate rows to be concise. Show each brand's figure for the new period; if a brand from the previous answer has no figure in the retrieved data for the new period, keep its row and mark it "not in the indexed data" rather than silently omitting it. Assume US geography and USD. Do NOT add any follow-up offer about other periods. Answer the user's question:{Topic.Query2}
                      fileSearchDataSource:
                        searchFilesMode:
                          kind: DoNotSearchFiles
                      knowledgeSources:
                        kind: SearchSpecificKnowledgeSources
                        knowledgeSources:
                          - cr1df_FinSightUSV2.topic.FinSightUSV2AzureAISearch_H2igSam_KOdvKH_SmGW4f
                      applyModelKnowledgeSetting: false
              elseActions:
                - kind: SetVariable
                  id: setVariable_newQMonth
                  variable: Topic.Query3
                  value: =Topic.FollowupReply & " (same brands and context as the previous answer)"
                - kind: SearchAndSummarizeContent
                  id: search_newQMonth
                  userInput: =Topic.Query3
                  additionalInstructions: |-
                    You answer US financial questions ONLY from the retrieved Financial Results content (US monthly Financial Close decks) - the source of truth for $ figures (Net Sales, Cost, Gross Margin, OPEX, Operating Income). GROUNDING: Quote numbers VERBATIM (USD). Never fabricate, compute, or estimate. If the retrieved content does not contain the figure, say "I do not have that in the indexed financial results" and stop. Keep the SAME format as the previous answer (rankings: Brand | Net Sales ($m) | vs PY (%), every row showing BOTH the Net Sales and the vs PY %). Assume US geography and USD. Do NOT add any follow-up offer about other periods. Answer the user's question:{Topic.Query3}
                  fileSearchDataSource:
                    searchFilesMode:
                      kind: DoNotSearchFiles
                  knowledgeSources:
                    kind: SearchSpecificKnowledgeSources
                    knowledgeSources:
                      - cr1df_FinSightUSV2.topic.FinSightUSV2AzureAISearch_H2igSam_KOdvKH_SmGW4f
                  applyModelKnowledgeSetting: false
    - kind: EndDialog
      id: 6H3lJ5
inputType: {}
outputType: {}
